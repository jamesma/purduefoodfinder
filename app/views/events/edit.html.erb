<% provide(:title, @event.name) %>
<h1>Edit Event</h1>

<div class="row">
  <div class="span6 offset3">
    <%= simple_form_for(@event, html: { class: 'form-vertical' }) do |f| %>
      <%= f.error_notification %>

      <%= f.input :name, required: false, input_html: {size: 50, maxlength: 50} %>
      <%= f.input :details, required: false, input_html: {size: 250, maxlength: 250} %>
      <%= f.input :where, required: false, input_html: {size: 50, maxlength: 50} %>
      <%= f.input :source, required: false, input_html: {size: 50, maxlength: 50} %>
      <%= f.input :category, required: false, collection: @category_options %>
      <%= f.input :whendate, required: false, as: :custom_datepicker, label: 'Date' %>
      <%= f.input :whentime, required: false, label: 'Time' %>

      <%= f.button :submit, 'Submit', :class => 'btn btn-large btn-primary' %>
    <% end %>
  </div>
</div>